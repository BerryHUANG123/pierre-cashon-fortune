## 2026-05-16 工作记录

### 项目
pierre-cashon-fortune (GitHub Pages: https://berryhuang123.github.io/pierre-cashon-fortune/)

### 执行的三项架构重构

#### 第一步：粒子特效函数抽象化 (commit: 79e0ba2)
- 将20+高度重复的皮肤特效函数统一为 `ParticleEffectEngine` 类
- 使用预设(preset)模式，支持6种皮肤特效
- 预计减少代码约350行
- 文件位置：`C:/Users/黄进阳/WorkBuddy/2026-05-16-task-3/index.html`

#### 第二步：CSS变量化皮肤系统 (commit: cea4c3b)
- 在 `:root` 中定义CSS变量（--skin-*）
- 重写 `applySkin()` 函数，通过 `style.setProperty()` 动态设置CSS变量
- 更新所有相关样式使用CSS变量
- 预计减少代码约80行

#### 第三步：GameState统一管理 (commit: 1dd26cc)
- 创建 `GameState` 类封装所有localStorage操作
- 支持数据版本迁移(v1->v2->v3)
- 提供兼容层函数 `saveData()` 和 `loadData()`
- 减少全局变量直接操作

#### 第四步：JS/CSS从HTML中分离 (commit: 966ecbd)
- 将2176行CSS代码提取到 `styles.css`
- 将3181行JavaScript代码提取到 `game.js`
- 更新 `index.html` 只保留206行HTML结构
- HTML文件减少96.3% (5555行 -> 206行)
- 提交信息：`refactor: 将CSS和JS从HTML中分离为独立文件`

### Git提交历史
| Commit | 描述 |
|--------|------|
| 79e0ba2 | 粒子特效函数抽象化为ParticleEffectEngine类 |
| cea4c3b | CSS变量化皮肤系统 |
| 1dd26cc | GameState统一数据管理 |
| 966ecbd | 将CSS和JS从HTML中分离为独立文件 |

### 最终文件结构
```
index.html  - 206行 (原5555行, -96.3%)
styles.css  - 2169行 (新分离)
game.js    - 3181行 (新分离)
```

### 代码减少总计
- 粒子特效抽象化：~350行
- CSS变量化皮肤：~80行
- HTML/CSS/JS分离：HTML文件减少5350行
- **总计约5780行代码优化**

#### 第五步：修复 game.js 末尾残留 HTML 标签 (commit: 87578b1)
- 问题：`game.js` 末尾残留了 `</script></body></html>` 等 HTML 标签，导致浏览器报 `Uncaught SyntaxError: Unexpected token '<'`
- 修复：移除残留 HTML 标签，补回被误删的 `closeAchievements()` 函数定义
- JS 语法检查：`node --check game.js` 通过

### 用户要求：每次改完代码后自测
自测清单（以后每次改完代码都必须执行）：
1. `node --check game.js` — JS 语法检查
2. 检查 HTML 中 `onclick` 调用的函数是否都在 JS 中定义
3. 检查 CSS/JS 文件路径引用是否正确

#### 第六步：添加「我的物品」库存系统 (commit: 86b3d45)
- GameState 升级至 v4，新增 `inventory` 字段
- 商店道具购买后进入库存，不再立即生效
- 新增幸运符道具（稀有率 +15%）
- 新用户赠送新手礼包（加速卡×3、幸运符×1）
- 顶部工具栏新增📦仓库入口按钮

#### 第七步：接入 Supabase 云端数据同步 (commit: 239c99b)
- 技术栈：Supabase (PostgreSQL + Auth + REST API)
- 数据库表：game_data (id, user_id, data[jsonb], updated_at, created_at)
- RLS 启用：用户只能读写自己的数据
- 认证方式：邮箱+密码
- 同步策略：登录后静默拉取，saveData 自动上传，时间戳冲突解决
- 离线兼容：未登录时仍用 localStorage
- 配置文件：supabase-config.js（含 URL 和 anon Key）
- Supabase 项目：celufcywdbgoblmfdxgk
4. 看浏览器 Console 有无报错

#### 第八步：修复 Supabase 集成后的运行时错误 (commit: 90ea470)
- 问题1：`Identifier 'supabase' has already been declared` — supabase-config.js 中 `const supabase` 与 CDN 全局 `window.supabase` 命名空间冲突
  - 修复：将客户端实例变量重命名为 `supabaseClient`，game.js 中所有引用同步更新
- 问题2：`toggleHistory is not defined` — `window.toggleHistory = toggleHistory` 引用了不存在的函数
  - 修复：移除 toggleHistory、toggleSettings、changeSkin、resetGame 四个无效的 window 赋值（HTML 中未使用）
- 问题3：`Cannot access 'currentUser' before initialization` — 上两个错误导致脚本中断，`let currentUser` 未执行，但 `handleAuthBtnClick` 已被提升
  - 修复：前两个错误修复后此问题自动解决

#### 第九步：修复 effectAnimationId 未定义错误 (commit: edf9db0)
- 问题：`stopEffect` 函数被定义了两次——新版（第891行，调用 particleEngine.stop()）和旧版（第2020行，引用不存在的 effectAnimationId）。JS 函数声明后定义覆盖前定义，导致新版被旧版覆盖
- 修复：删除旧版重复代码块（initEffectCanvas、resizeEffectCanvas、stopEffect 三个函数），保留 ParticleEffectEngine 重构后的新版
- 额外验证：确认 game.js 中无其他重复函数声明
